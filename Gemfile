source 'http://rubygems.org'

ruby '2.4.1'

# Server
gem 'puma'

# App
gem 'sinatra'
gem 'httparty'
gem 'rack-cache', require: 'rack/cache'

# Assets
gem 'sprockets'
gem 'coffee-script'
gem 'sass'

group :development do
  gem 'shotgun'
  gem 'awesome_print'
end

group :production do
  gem 'newrelic_rpm'
end
