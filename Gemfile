source 'http://rubygems.org'

ruby '2.0.0'

# Server
gem 'unicorn'

# App
gem 'sinatra'
gem 'httparty'
gem 'rack-cache', require: 'rack/cache'

# Assets
gem 'sprockets'
gem 'coffee-script'
gem 'stylus'

group :development do
  gem 'shotgun'
  gem 'awesome_print'
end

group :production do
  gem 'newrelic_rpm'
end
